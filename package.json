{
  "name": "respan-mcp",
  "version": "1.0.0",
  "description": "Respan MCP Server - HTTP streaming and local stdio modes",
  "main": "dist/lib/index.js",
  "type": "module",
  "bin": {
    "respan-mcp": "dist/lib/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "stdio": "npm run build && node dist/lib/index.js",
    "start": "node dist/lib/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^5.5.16",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
